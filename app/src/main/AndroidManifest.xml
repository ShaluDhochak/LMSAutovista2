<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.lmsautovista">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.Activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Activity.LoginActivity" />
        <activity
            android:name=".View.Activity.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity android:name=".View.Activity.CallingTaskActivity" />
        <activity android:name=".View.Activity.NewLeadDashboardActivity" />
        <activity android:name=".View.Activity.UnassignedDashboardLeadActivity" />
        <activity android:name=".View.Activity.CallTodayDashboardActivity" />
        <activity android:name=".View.Activity.PendingNewLeadActivity" />
        <activity android:name=".View.Activity.PendingFollowUpDashboardActivity" />
        <activity android:name=".View.Activity.DashboardCountDetailActivity" />
        <activity android:name=".View.Activity.CustomerDetailActivity"></activity>
    </application>

</manifest>